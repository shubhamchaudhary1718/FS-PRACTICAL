<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tax Form - Income Calculator</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .main-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
        }
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 10px;
            padding: 12px 30px;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
        }
        .result-card {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            color: white;
            border-radius: 15px;
            padding: 30px;
            text-align: center;
            margin-top: 30px;
        }
        .error-message {
            background: #ffe6e6;
            border: 1px solid #ff9999;
            border-radius: 8px;
            padding: 10px;
            margin-bottom: 15px;
            color: #cc0000;
        }
        .income-input {
            position: relative;
        }
        .income-input i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #667eea;
        }
        .income-input .form-control {
            padding-left: 45px;
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="main-container p-5">
                    <div class="text-center mb-4">
                        <i class="fas fa-calculator fa-3x text-primary mb-3"></i>
                        <h1 class="h2 mb-3">Tax Form Calculator</h1>
                        <p class="text-muted">Enter your income from two sources to calculate the total</p>
                    </div>

                    <% if (errors && errors.length > 0) { %>
                        <div class="alert alert-danger">
                            <h6><i class="fas fa-exclamation-triangle me-2"></i>Please fix the following errors:</h6>
                            <ul class="mb-0">
                                <% errors.forEach(error => { %>
                                    <li><%= error.msg %></li>
                                <% }); %>
                            </ul>
                        </div>
                    <% } %>

                    <form method="POST" action="/calculate" class="needs-validation" novalidate>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="income1" class="form-label">
                                    <i class="fas fa-dollar-sign me-2"></i>Income Source 1
                                </label>
                                <div class="income-input">
                                    <i class="fas fa-money-bill-wave"></i>
                                    <input 
                                        type="number" 
                                        class="form-control <%= errors && errors.find(e => e.path === 'income1') ? 'is-invalid' : '' %>" 
                                        id="income1" 
                                        name="income1" 
                                        step="0.01" 
                                        min="0" 
                                        placeholder="0.00"
                                        value="<%= formData ? formData.income1 : '' %>"
                                        required
                                    >
                                </div>
                                <div class="form-text">Enter your income from the first source (e.g., salary, business)</div>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label for="income2" class="form-label">
                                    <i class="fas fa-dollar-sign me-2"></i>Income Source 2
                                </label>
                                <div class="income-input">
                                    <i class="fas fa-money-bill-wave"></i>
                                    <input 
                                        type="number" 
                                        class="form-control <%= errors && errors.find(e => e.path === 'income2') ? 'is-invalid' : '' %>" 
                                        id="income2" 
                                        name="income2" 
                                        step="0.01" 
                                        min="0" 
                                        placeholder="0.00"
                                        value="<%= formData ? formData.income2 : '' %>"
                                        required
                                    >
                                </div>
                                <div class="form-text">Enter your income from the second source (e.g., investments, rental)</div>
                            </div>
                        </div>

                        <div class="text-center">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="fas fa-calculator me-2"></i>Calculate Total Income
                            </button>
                        </div>
                    </form>

                    <% if (totalIncome !== null) { %>
                        <div class="result-card">
                            <i class="fas fa-chart-line fa-2x mb-3"></i>
                            <h3 class="mb-3">Total Income Calculated</h3>
                            <div class="display-4 fw-bold mb-2">$<%= totalIncome %></div>
                            <p class="mb-0">
                                <small>
                                    Income Source 1: $<%= parseFloat(formData.income1).toFixed(2) %> | 
                                    Income Source 2: $<%= parseFloat(formData.income2).toFixed(2) %>
                                </small>
                            </p>
                        </div>
                    <% } %>

                    <div class="text-center mt-4">
                        <small class="text-muted">
                            <i class="fas fa-shield-alt me-1"></i>
                            All calculations are performed securely on the server
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
